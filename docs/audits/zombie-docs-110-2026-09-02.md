# Zombie docs pass — 1.10 prep (2026-09-02)

**Mode:** content preparation for the upcoming 1.10 release (website lane, task `0ddcbbfc`)
**Window:** faber 1.9.0 release era → current main tips. Start: radix `v0.84.0` / `63b706156`
(`faber v1.9.0 + radix v0.84.0`, 2026-08-26); ends: radix `61dac24be`, faber `563226c`,
norma `51399cd` (2026-09-01). ~936 radix commits, 2 faber grammar commits, 2 norma commits.
**Authorities:** `faber/docs/EBNF.md` (era `62135b105` → tip), radix corpus + `corpus/index.toml`,
`radix/locale/*` packs, `norma` src + packaging, radix commits in-window, live GitHub publish
state of `faberlang/releases`, radix release docs (`docs/release/faber/v1.9.0.md`, `v0.84.0.md`).
**Toolchain precondition:** installed `faber` is 1.8.0 — older than both the 1.9.0 era and the
upcoming 1.10 this pass prepares for. Per lane charter: no site build, no self-install. All
Speculum/dist regeneration is flagged as residual. Edited Faber fences were instead validated
with `validate-fences.sh` against a packet build of radix current main (fresh tree, 26/26 pass).

## Delta derived from the git windows

| Surface | Change | Authority |
| --- | --- | --- |
| Grammar | `reice_stmt ::= 'reice' expression 'iace' expression` added to `statement_core` — reject guard, boolean opposite of `requirit` (en `reject`/`require`); lowers to `si (cond) { iace err }`; needs `⇥` channel | faber `288cc94` (EBNF [129]), radix `de60daa5c` (LSR-W2), en pack `requirit="require"`, `reice="reject"`, corpus `meta/reice.fab` |
| Grammar | `closure_modifier ::= 'libera' \| 'nucleum'` — kernel closure: device-safe signature subset, no error channel, no host effects, no escapes | faber `e457865` (EBNF [029], KCM-08), radix `0dc6972c0` (KCM-03 contract) |
| Device | Only *public* `@ nucleum` functions are launchable entries; private `@ nucleum` stays a module-internal helper | radix `b3234bc57` (DFV2-1) |
| Loops | `itera ex a, b apud [i] fixum x, y` — N-ary lockstep walk; lista-only sources, name binders, first source owns length (mismatch errors, never truncates) | radix `fc05e83b6`, EBNF `itera_stmt`/`apud_clause`, corpus `itera/lockstep-n2|n3|at-index` |
| Stdlib | Norma ships as a Faber package with owned locale fragments (packaging only; no `norma/src` change) | norma `6f1fdb6`, `51399cd` |
| Locale | Reader packs now live in `radix/locale/` (8 packs; site already followed the move in `c677654b`) | radix `04fe5d5dc` |
| Release | Faber 1.9.0 shipped locally 2026-08-26 (status final, tag `faber/v1.9.0`, companion radix 0.84.0). **Not yet published** to the public `faberlang/releases` repo — latest public release remains `faber-v1.8.0` (macOS arm64 published 2026-08-24; Linux x64 attached 2026-08-25) | radix `docs/release/faber/v1.9.0.md`, `release-manifest.yaml`, GitHub API `releases/latest` |
| Matrix | `EBNF_MATRIX.md` and `docs/design/target-capability-matrix.md` unchanged in window | git diff empty |

## Fixed rows (commit `b70ea976`)

| Claim (site) | Authority checked | Change | Commit |
| --- | --- | --- | --- |
| No mention of guard statements; `reice` absent everywhere | EBNF [128]/[129]; radix `de60daa5c`; en pack spellings; `corpus/meta/reice.fab` | `language/errors.md`: new "Guards — requirit / reice" section with both forms + semantics; `cheatsheet/errors.md`: compact "Guards" section | `b70ea976` |
| `itera ex` documented single-collection only | radix `fc05e83b6`; EBNF `itera_stmt`/`apud_clause`; `corpus/itera/lockstep-*` | `cheatsheet/loops.md`: new "In lockstep" subsection (series + `apud [i]`, arity/length law) | `b70ea976` |
| `faber run --backend` "launches `@ nucleum` kernels" without visibility law | radix `b3234bc57` (DFV2-1); KCM-03 `0dc6972c0` | `toolchain/compiling.md` device footnote: only public `@ nucleum` functions are entries; kernel-closure device-safe contract sentence | `b70ea976` |
| `llms.txt` "Current CLI release: Faber 1.6.0" + 1.6.0 asset table + "Install faber 1.6.0" | GitHub API: latest public release `faber-v1.8.0` (published 2026-08-24; Linux leg 2026-08-25) | `static/llms.txt`: all three spots → 1.8.0 with public asset URLs | `b70ea976` |
| `agents/index.md` "Install the CLI (current: 1.6.0)" + 1.6.0 URLs | same | → 1.8.0 + public URLs | `b70ea976` |
| `start/install.md` current-release table pinned 1.6.0 (published 2026-08-10, bundled "Radix 0.81.0") + 1.6.0 archive URLs | radix `docs/release/faber/v1.8.0.md` (radix 0.83.0 companion); GitHub API asset facts | → 1.8.0 / 2026-08-24 / Radix 0.83.0; all archive + checksum URLs → public `faberlang/releases` 1.8.0 assets | `b70ea976` |
| `start/index.md` "current release (**1.6.0**)" ×2 | same | → 1.8.0 | `b70ea976` |
| `index.md` download block: links + checksum + "Assets on GitHub" pointed at **private** `faberlang/radix` repo (404 for the public); checksum `22d688cc…` matches no public asset; "lists one prebuilt archive"; no Linux row; "published 2026-08-18" | GitHub API `releases/latest`: public digests `dd584ebf…` (macOS), `1b64b591…` (Linux); publish dates | Rewrote to public repo URLs, verified digests, both platform rows, publish date 2026-08-24; Radix sub-block private link replaced with text (closed source, ships inside Faber) | `b70ea976` |
| `dist/llms.txt`, `dist/agents/index.md` stale vs `static/` | build-site.sh copy step (static → dist, no toolchain) | Mechanically synced copies (not a render; HTML pages remain stale — see residuals) | `b70ea976` |

Proof: `validate-fences.sh` on the four edited pages — 26/26 fences pass against packet radix
current main (`/tmp/website-fences.log`, `/tmp/website-fences2.log`).

## Residual rows (flagged, not guessed)

| Claim / gap | Why unresolved | Owner / unblock |
| --- | --- | --- |
| No `faber-1.9.0` release page; 1.9.0 absent from download blocks and "current release" rows | `generate-releases.py` is the by-hand release-time step (needs `gh` + network), and 1.9.0 has **no public GitHub release** yet — download rows would 404. Publish is operator-gated. After publish: rerun `generate-releases.py`, then bump index/install/llms version rows to 1.9.0 (radix 0.84.0 companion). | Release lane / operator at publish |
| Missing 1.9.0-era site updates generally (tensor glyphs, equality ladder ≅/≈ tiers, container-bounds, kernel/reduction wave, MIR completeness) | Those shipped **in** 1.9.0 (before this task's window start). They are release-page content, not in-window deltas; also unverifiable in detail without the fresh toolchain. | Release lane with the 1.9.0/1.10 changelog pass |
| All `dist/*.html` (grammar page missing `reice`/`nucleum` closure rule, target matrix, targets lanes, examples incl. device/kernel entry wording, corpus pages behind `registry_terms` 201 / `fab_count` 484 at tip, localization, `llms-full.txt` "(171 canonical terms)") | Regeneration needs the Speculum build; charter precondition fails (installed faber 1.8.0 < documented release). Never hand-edit output. | Site build after the release lane installs a ≥1.10 faber |
| `dist/en-US/syntax/` (12 pages) and other retired paths have no `src/` counterpart | Generated-output hygiene question (prune step), not a content claim; dist is out of write scope for hand edits. | Mind / build-system owner |
| `examples/*.md` device examples (`device-summa`, `mlp-forward`) say "a function marked `@ nucleum` is a compute kernel" without the public/private entry law | Pages are step-0 generated from the `examples/` sibling repo — outside this lane's write scope. | examples repo authors (add `publica` visibility to kernel fixtures) / regeneration |
| `language/errors.md` "Current status": "Rust and Go lowering for full `⇥`/`iace`/`cape` runtime behaviour is still a backend gap" | Pre-window claim; 1.9.0's MIR-completeness wave does not by itself prove Rust/Go failable lowering moved. No contradicting authority found; left untouched rather than guessed. | Verify against e2e rust/go lanes when the fresh toolchain lands |
| `AGENTS.md` "Binary versions: faber 1.6.0 / radix 0.81.0" | Repo meta-doc, not site content; out of this task's site-content write scope. | Site repo maintainers |
| Norma-as-Faber-package (norma `6f1fdb6`) not reflected in install/prose pages | No existing site claim contradicts it (`start/projects.md` already says `norma:*` resolves from Cista); nothing stale to fix, and a new "install norma" section would be new-feature authoring beyond this pass. | Norma/library pages at 1.10 content pass |

No push, no publish, no deploy, no CI changes — commits on `factory/website` only.
