يمتلك فابر ثلاث كلمات مفتاحية للمتغيرات ورمز تخصيص مخصص. الفرق الجوهري
هو بين `fixum` (كتابة واحدة) و `varia` (قابلة لإعادة الإسناد بحرية)،
وبين `←` (تدفق تنفيذي) و `=` (شكل حقل بنيوي).

## fixum — ربط غير قابل للتغيير {#fixum-immutable-binding}

روابط `fixum` تُكتب مرة واحدة. يجوز التصريح بها بمُهيئ أو بدونه؛
وإذا صُرح بها بدونه، فيجب إسنادها مرة واحدة بالضبط قبل القراءة.
أي إسناد ثانٍ يُرفض.

<<<FENCE 0>>>

التهيئة المؤجلة:

<<<FENCE 1>>>

## varia — ربط قابل للتغيير {#varia-mutable-binding}

روابط `varia` قابلة لإعادة الإسناد بحرية:

<<<FENCE 2>>>

## sit — سكر غير قابل للتغيير مع استدلال النوع {#sit-inferred-immutable-sugar}

`sit` هو سكر نحوي لـ `fixum _` — ربط غير قابل للتغيير مع نوع مُستدلّ:

<<<FENCE 3>>>

## الربط التنفيذي مقابل التعريف البنيوي {#runtime-binding-vs-structural-definition}

يفصل فابر ما تدمجه معظم اللغات في `=`:

| الرمز | الدور | يُستخدم لـ |
|-------|------|---------|
| `←` | تدفق تنفيذي | الربط الأولي، إعادة الإسناد، التعديل |
| `=` | شكل بنيوي | أسماء الحقول داخل القيم الحرفية والبيانات الوصفية |

<<<FENCE 4>>>

## استخراج الحقول بـ ex {#ex-field-extraction}

`ex` يستخرج الحقول من قيمة إلى روابط محلية:

<<<FENCE 5>>>

## الزيادة والنقصان الملحقان {#postfix-increment-and-decrement}

`⊕` و `⊖` هما عبارتا زيادة ونقصان ملحقتان للأماكن القابلة للتغيير
من نوع `numerus`. هما عبارات فقط — بلا قيمة تعبيرية، وبلا
أشكال سابقة:

<<<FENCE 6>>
