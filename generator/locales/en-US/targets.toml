# Localized chrome for tooling/targets.md (assembled by generate-target-matrix.py).
# Matrix tables stay language-neutral (Latin term/target ids + glyphs).

title = "Target compatibility"

intro = """
Faber is one language with many compilation contracts. This page is the
**measured lowerability matrix**: for each corpus term, which targets can
lower it, and at what support level.

Policy verbs (support / erase / warn / reject / defer) and pipeline routing
live on [Codegen targets](/tooling/codegen-targets.html). This page is the
large scannable row list — HIR application-lane targets and MIR systems-lane
targets side by side in the tables below.

Live CLI summary: `faber targets`.
"""

meta_generated = "**Generated**: {date} by `scripta/generate-ebnf-matrix.py` — **do not edit**."
meta_measurement = "**Measurement**: `emit_hir_target_matrix` + `emit_mir_target_matrix` (in-process, no external toolchains)."
meta_join = "**Join**: `examples/corpus/index.toml` terms → exempla."

official = """
This is the **official generated** grammar×target support matrix. It reports
**lowerability** — can target X lower grammar production Y — across every term in
the exempla corpus. Runtime semantics (erase/warn/defer policy verbs), per-target
contracts, and pipeline routing live in
[Codegen targets](/tooling/codegen-targets.html), which links here for the rows.
"""

legend_heading = "Legend"
legend_col_glyph = "Glyph"
legend_col_meaning = "Meaning"
legend_full = "fully supported — all analyzable exempla for the term lower"
legend_partial = "partial — some exempla lower, some have a measured gap"
legend_planned = "planned — not yet lowering; curated overlay (`scripta/ebnf-matrix-overrides.toml`)"
legend_unsupported = "not supported — no exempla lower; default-truth, measured gap is real"
legend_unmeasured = "not measured — no analyzable exempla for this term on this lane"

legend_note = """
A ✓ means the corpus exempla exercising this term lower to the target. It does
**not** guarantee identical runtime semantics. Some targets *erase* or *warn* on
certain constructs (e.g. Go erases borrow modes `de`/`in`/`ex`) — those still
render ✓ here because they lower. See the policy doc for that nuance.
"""

summary_heading = "Corpus-wide summary (all registered terms)"
app_lane = "Application lane (HIR → emitted source languages)"
sys_lane = "Systems lane (MIR → device/IR artifacts)"
col_target = "target"
col_capable = "capable"
col_analyzable = "analyzable"
col_pct = "%"
col_term = "term"

keywords_app = "Keywords — application lane"
operators_app = "Operators — application lane"
keywords_sys = "Keywords — systems lane"
operators_sys = "Operators — systems lane"
other_terms = "Other terms (`existing-home` / unspecified)"
h3_keyword = "keyword"
h3_operator_group = "operator-group"
h3_existing_home = "existing-home"
