ฟังก์ชัน parse(ข้อความ raw) → จำนวน ⇥ ข้อความ {
    ถ้า raw ≡ "" {
        โยน "empty input"
    }
    คืน raw ↦ จำนวน ⇥ 0
}

ฟังก์ชัน duplum(ข้อความ raw) → จำนวน ⇥ ข้อความ {
    คืน parse(raw) * 2
}

เริ่ม {
    ทำ {
        บันทึก duplum("21")
    }
    จับ err {
        เตือน err
    }
}
