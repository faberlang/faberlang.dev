دالة parse(نص raw) → عدد ⇥ نص {
    إذا raw ≡ "" {
        ارم "empty input"
    }
    أعد raw ↦ عدد ⇥ 0
}

دالة duplum(نص raw) → عدد ⇥ نص {
    أعد parse(raw) * 2
}

بداية {
    افعل {
        اعرض duplum("21")
    }
    التقط err {
        نبه err
    }
}
