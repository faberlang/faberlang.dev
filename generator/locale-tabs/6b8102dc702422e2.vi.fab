kiểu Persona {
    văn_bản nomen
    số aetas
}

hàm descriptio(Persona p) → văn_bản {
    trả p.nomen
}

bắt_đầu {
    hằng Persona p ← Persona {nomen = "Marcus", aetas = 30}
    ghi_chú descriptio(p)
}
