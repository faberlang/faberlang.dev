بداية {
    ثابت قائمة<نص> raw ← ["1", "two", "3"]
    افعل {
        كرر من raw ثابت item {
            ثابت عدد n ← item ↦ عدد
            اعرض n
        }
    }
    التقط err {
        نبه err
    }
}
