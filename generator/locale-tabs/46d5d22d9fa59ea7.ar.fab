دالة divide(عدد a, عدد b) → عدد ⇥ نص {
    إذا b ≡ 0 {
        ارم "division by zero"
    }
    أعد a / b
}

دالة tutum(عدد a, عدد b) → عدد {
    افعل {
        أعد divide(a, b)
    }
    التقط err {
        نبه err
        أعد 0
    }
}

بداية {
    اعرض tutum(10, 2)
    اعرض tutum(10, 0)
}
