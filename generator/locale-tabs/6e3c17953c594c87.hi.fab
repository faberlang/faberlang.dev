फलन parse(पाठ raw) → संख्या ⇥ पाठ {
    यदि raw ≡ "" {
        इधरफेंको "empty input"
    }
    लौटाओ raw ↦ संख्या ⇥ 0
}

फलन duplum(पाठ raw) → संख्या ⇥ पाठ {
    लौटाओ parse(raw) * 2
}

आरंभ {
    करो {
        दिखाओ duplum("21")
    }
    पकड़ो err {
        चेताओ err
    }
}
